---
import * as stylex from '@stylexjs/stylex'
import { styles } from './Kicker.styles'

interface Props {
  label: string
}

const { label } = Astro.props
---

<p {...stylex.props(styles.kicker)}>
  <span aria-hidden="true" {...stylex.props(styles.dot)}></span>{label}
</p>
