/* The install command, stated once.
 *
 * The page renders it three times: as the line the hero shows, as the payload
 * the hero's copy control writes, and again in the last section through the
 * markdown pipeline. One string behind all three is what keeps a reader from
 * ever copying a command the page does not show.
 */

export const INSTALL_COMMAND = 'curl -fsSL niwa.rs | sh'
