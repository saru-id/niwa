---
import { FUNCTIONS } from '../../../data/functions'
import { pageLink } from '../../../data/links'
import ApiPage from '../_ApiPage.astro'

const deck =
  'Eight calls that are not resources. They return values, load code, or define a kind of your own, and none of them produces a plan line.'
const next = [pageLink('/guides/custom-resource'), pageLink('/reference/api/facts')]
---

<ApiPage title="Luau utilities" deck={deck} entries={FUNCTIONS} next={next} />
